node_modules/

# Local environment overrides. VITE_API_BASE points a checkout at an API;
# it is per-developer and must never be committed.
.env.local
.env.*.local
dist/
coverage/
*.tsbuildinfo

# editors
.idea/
.vscode/
*.swp

# os
.DS_Store
Thumbs.db

# large local artefacts — published releases live in /data, not in git
*.las
*.laz
*.tif
*.pmtiles
/pipeline/.venv/
/pipeline/work/

# build products — artefacts are rebuilt, not committed
/data/

# python
__pycache__/
*.py[cod]
*.egg-info/
.coverage
.coverage.*
.pytest_cache/

# A slide deck is a build product of a meeting, not of this repository.
docs/*.pptx
