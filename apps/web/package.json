{
  "name": "@drainlens/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The browser application. Holds the address, task and scenario inputs in memory for the life of the tab and nowhere else.",
  "scripts": {
    "build": "tsc --build"
  },
  "dependencies": {
    "@drainlens/schema": "0.1.0",
    "@drainlens/scenario": "0.1.0"
  }
}
